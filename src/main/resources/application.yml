spring:
  datasource:
    url: jdbc:mysql://database-12.cfqya08y6trr.ap-northeast-2.rds.amazonaws.com:3306/qqq
    username: admin
    password: kaka123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
    org.springframework.transaction: debug
